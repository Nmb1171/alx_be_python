# Capture the user's age as input (as a string)
age_str = input('How old are you? ')

# Convert the input to an integer
age = int(age_str)

# Add 27 to the age
age += 25

# Display the prophecy
print(f'In 2050, you will be {age} years old.')
